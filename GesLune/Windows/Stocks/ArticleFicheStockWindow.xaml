<Window x:Class="GesLune.Windows.Articles.ArticleFicheStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GesLune.Windows.Articles"
        mc:Ignorable="d"
        Title="ArticleFicheStockWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="MainDataGrid"
                  Grid.Row="0"
                  AutoGenerateColumns="True"
                  ItemsSource="{Binding FicheStock}"
                  >
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="Identifiant d'article"
                                    Binding="{Binding Etat_Stock_Article_Id}"
                                    Visibility="Hidden"/>
                <DataGridTextColumn Header="Nom d'article"
                                    Binding="{Binding Etat_Stock_Article_Nom}"
                                    />
                <DataGridTextColumn Header="Stock disponible"
                                    Binding="{Binding Etat_Stock_Quantity}"
                                    />
            </DataGrid.Columns>-->
        </DataGrid>
    </Grid>
</Window>
